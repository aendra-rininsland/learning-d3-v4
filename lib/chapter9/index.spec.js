/* eslint-disable import/no-extraneous-dependencies */
/* eslint-env mocha */
import chai from 'chai';
import tschart from './index.ts';

chai.should();

describe('functional tests for UK election sankey', () => {
  // describe('zoom() method', () => {
  //   it('should redraw when supplied an argument', () => {
  //     tschart.zoom('CON');
  //   });
  //
  //   it('should unzoom when no argument supplied', () => {
  //     tschart.zoom();
  //   });
  // });
});
